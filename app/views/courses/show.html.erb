<h1>Show Page of a specific course</h1>
<nav class="nav">
  <%= link_to "Home", root_path %>
  <% if user_signed_in? %>
  <li>
  <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
  </li>
  <% else %>
  <li>
  <%= link_to('Login', new_user_session_path)  %>
  </li>
  <% end %>

</nav>


<ul id="allcourses">
    <div class="grey">
      <li>Subject: <%= @course.subject %></li>
    </div>
</ul>
